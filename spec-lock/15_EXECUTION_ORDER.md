# V3 Execution Order — FROZEN RC1

## Pre-build
P0. Reconcile/freeze spec pack — RC1 complete.
P1. Gemini independent red-team — complete.
P2. Claude independent red-team — complete.
P3. Reconcile findings — complete in `20_RED_TEAM_RECONCILIATION.md`.
P4. Create technically protected external acceptance harness.
P5. Run five risk spikes.
P6. Freeze provider/cloud choices and measured thresholds.
P7. Create GitHub implementation repo + CI/branch protections.
P8. Run final contradiction/consistency gate; only then set `CURRENT_PHASE: PHASE_0`.

## Main build
### Phase 0 — Architecture/repo/contracts/testing
Monorepo, config, canonical schemas/hash module/error enum, PostgreSQL migrations, isolated testing, external acceptance integration, build-state protocol.

### Phase 1 — Workspace + Product Truth
Server IDs, CAS ProductRevision, snapshots, verification/provenance/hash domains, HTTP authority.

### Phase 2 — Physical Assets + RawAssetManifest
Upload/storage, byte SHA, decode/probe, roles/ownership, READY-only immutable physical manifest.

### Phase 3 — Multimodal Asset Intelligence + AnalysisContext + Product Intelligence
Real media, exact cache identity, structured evidence, exact frozen context, Product intelligence/review.

### Phase 4 — Creative Engine
Exactly-six production pack, semantic gate, full claim extraction/grounding, evidence gates, atomic replacement, pack concurrency.

### Phase 5 — Real Voice + Alignment + MasterTimeline
Real TTS, real ASR, frozen Arabic normalization profile/thresholds, duration fit, cache/dedupe, target-specific stale guards, timeline.

### Phase 6 — Scene Director
Exact asset/segment binding, safe manual overrides, coherence validation.

### Phase 7 — AI Visuals / Veo
Selective usage, DB-first paid-op claim, durable provider operation/restart reconciliation.

### Phase 8 — Render
Canonical render gate, FFmpeg, RenderJob, physical probe/hash validation.

### Phase 9 — QA / Download / Export
Exact-output QA using APPROVED, production download, queue, ZIP/manifest export.

### Phase 10 — Cloud hardening / analytics / winner iteration
Production deployment, monitoring, security, analytics lineage and winner iteration.
